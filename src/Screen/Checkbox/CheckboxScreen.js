import { 
    View,
    Text,
    ScrollView,
} from 'react-native'
import React from 'react'

import {
    Checkbox,
} from "../../component"

const CheckboxScreen = () => {
  

  return (
    <ScrollView>
        <Checkbox/>
    </ScrollView>
  )
}

export default CheckboxScreen